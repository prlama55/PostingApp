query Products( $partnerId: String!) {
    products(partnerId: $partnerId) {
        id
        name
        price
        description
        createdAt
        partner {
            id
            clientId
            name
        }
    }
}
