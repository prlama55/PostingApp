query PartnerDetail($id: String!) {
    partner(id: $id) {
        id
        name
        emails
        partnerId
        createdAt
        payerId
        clientId
        verifiedAccount
        user {
            email
            name
        }
    }
}
